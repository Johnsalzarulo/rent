<ul class="nav nav-pills">
    <li class="active"><a href="#profile" data-toggle="tab" aria-expanded="true">Active</a></li>
  <li class=""><a href="#home" data-toggle="tab" aria-expanded="false">Past</a></li>
  <li class=""><a href="#potential" data-toggle="tab" aria-expanded="false">Potential</a></li>
  <li class=""><%= link_to 'Set Up a new Tenant', new_tenant_path %></li>
</ul>
<div id="myTabContent" class="tab-content">
    <div class="tab-pane fade active in" id="profile"><br>
    <ul class="list-group">
    <% @active_tenants.each do |tenant| %>
        <li class="list-group-item">
            <h4><%= link_to tenant.name, tenant %></h4>
            <h5><%= link_to tenant.property.address, tenant.property %></h5>
            <h6><a href="mailto:<%= tenant.email %>"><%= tenant.email %></a>  |  <%= tenant.phone %></h6>
            <%= link_to 'Detail', tenant, class:"btn btn-warning btn-xs" %>
        </li>
    <% end %>
    </ul>
  </div>
  <div class="tab-pane fade" id="home"><br>
    <ul class="list-group">
        <% @past_tenants.each do |tenant| %>
        <li class="list-group-item">
            <h4><%= link_to tenant.name, tenant %></h4>
            <h5><%= link_to tenant.property.address, tenant.property %></h5>
            <h6><a href="mailto:<%= tenant.email %>"><%= tenant.email %></a>  |  <%= tenant.phone %></h6>
            <%= link_to 'Detail', tenant, class:"btn btn-warning btn-xs" %>
        </li>
    <% end %>
        </ul>
  </div>
  <div class="tab-pane fade" id="potential"><br>
    <ul class="list-group">
         <% @potential_tenants.each do |tenant| %>
        <li class="list-group-item">
            <h4><%= link_to tenant.name, tenant %></h4>
            <h5><%= link_to tenant.property.address, tenant.property %></h5>
            <h6><a href="mailto:<%= tenant.email %>"><%= tenant.email %></a>  |  <%= tenant.phone %></h6>
            <%= link_to 'Detail', tenant, class:"btn btn-warning btn-xs" %>
        </li>
    <% end %>
        </ul>
  </div>
</div>
