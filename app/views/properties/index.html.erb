
<ul class="nav nav-pills">
	<li class="active"><a href="#profile" data-toggle="tab" aria-expanded="true">Occupied</a></li>
  <li class=""><a href="#home" data-toggle="tab" aria-expanded="false">Unoccupied</a></li>
  <li class=""><%= link_to 'Set Up a new Property', new_property_path %></li>
</ul>
<div id="myTabContent" class="tab-content">
	<div class="tab-pane fade active in" id="profile"><br>
    <ul class="list-group">
    <% @active_properties.each do |property| %>
        <li class="list-group-item">
        	  				<span class="badge">Issues: <%= property.issues.count %></span>
        	<h4><%= link_to property.address, property_path(property.id) %>
        	</h4>
        </li>
    <% end %>
	</ul>
  </div>
  <div class="tab-pane fade" id="home"><br>
    <ul class="list-group">
    <% @vacant.each do |property| %>
        <li class="list-group-item">
        	  				<span class="badge">Issues: <%= property.issues.count %></span>
        	<h4><%= link_to property.address, property_path(property.id) %>
        	</h4>
        </li>
    <% end %>
		</ul>
  </div>
</div>
