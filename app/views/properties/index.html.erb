<h1>Properties</h1>

    <% @properties.each do |property| %>
        <%= link_to property.address, property_path(property.id) %><br>
    <% end %>


<br>
<%= link_to 'Create a new Property', new_property_path %>
